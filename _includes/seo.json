{
  "@context": {
    "@vocab": "http://schema.org/",
    "@base": "{{site.url}}"
  },
  "@graph": [
    {% include seo/person.json %},
    {% include seo/website.json %},
    {% include seo/webpage.json %}
  ]
}
